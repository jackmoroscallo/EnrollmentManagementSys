@model IEnumerable<EnrollmentManagementSys.Models.SchoolYear>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SchoolYearName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SchoolYearName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SchoolYearID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SchoolYearID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SchoolYearID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
